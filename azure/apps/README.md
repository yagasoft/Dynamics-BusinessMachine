# Azure Application Artifacts

Future deployable Azure workloads for DBM belong here.

R0 intentionally leaves this folder empty. The Azure deployment workflow treats an empty app tree as a valid no-op so the OIDC and Key Vault contract can be established before the first Azure workload lands.
