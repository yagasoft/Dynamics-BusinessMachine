{
  "name": "dbm-designer-core",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm --prefix ../dbm-contract run build && tsc -p tsconfig.json",
    "test": "npm --prefix ../dbm-contract run build && tsx --test test/designer-core.test.ts",
    "validate": "npm run build && npm run test"
  },
  "dependencies": {
    "ajv": "8.18.0"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "tsx": "4.21.0",
    "typescript": "6.0.2"
  }
}
