{
  "name": "dbm-dataverse-synthesis",
  "version": "0.0.0",
  "private": true,
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "npm --prefix ../dbm-contract run build && npm --prefix ../dbm-process-experience run build && tsc -p tsconfig.json",
    "test": "npm --prefix ../dbm-contract run build && npm --prefix ../dbm-process-experience run build && tsx --test test/dataverse-synthesis.test.ts",
    "validate": "npm run build && npm run test"
  },
  "dependencies": {
    "xmlbuilder2": "4.0.3"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "tsx": "4.21.0",
    "typescript": "6.0.2"
  }
}
